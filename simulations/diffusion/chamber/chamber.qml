import QtQuick 2.0
import "../../.."

Simulation {
    name: "Chamber"
    description: "The blue atoms in this simulation have been trapped behind " +
                 "a membrane. This has now become permeable and the atoms are " +
                 "free to move out again."
}

